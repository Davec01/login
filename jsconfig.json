{
  "compilerOptions": {
    "baseUrl": ".", // Asegura que el alias se resuelva desde la raíz del proyecto
    "paths": {
      "@/*": ["./src/*"],
      "@/components/*": ["./components/*"] // Agrega esta línea para incluir la carpeta de componentes
    }
  }
}
